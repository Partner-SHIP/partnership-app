```plantuml
@startuml LoginPageViewModel

package viewmodel {
    class   LoginPageViewModel {
        -- fields --
        - LoginPageModel _model
        -- methods --
        + LoginPageViewModel()
        -- getters --
        + LoginPageModel()
    }
}
@enduml
```
```plantuml
@startuml SignInPageViewModel

package viewmodel {
    class   SignInPageViewModel {
        -- fields --
        - SignInPageModel _model
        -- methods --
        + SignInPageViewModel()
        -- getters --
        + SignInPageModel()
    }
}

@enduml
```